在 dev 分支执行「第一期 A:轻量梳理」(视觉语言不变):

1. 新增公共 UI 组件(frontend/src/components/ui/):AppModal(统一弹窗,替换 6 处手写 overlay)、AppPagination(统一分页)、EmptyState(统一空状态)、StatCard(统计格)
2. Dashboard 双栏分组:概览行(服务器状态+趋势图 md:grid-cols-2)→ 聊天室全宽 → 账户绑定区(MC+QQ、基岩+资料 lg:grid-cols-2)→ 邀请全宽;input+按钮行加 flex-wrap;删未用函数
3. Admin:settings 拆为「外观设置」「系统设置」两个 tab;4 处手写弹窗换 AppModal;2 套内联分页换 AppPagination;统计格换 StatCard;修 input-sm 失效类
4. EmptyState 替换 5 处(ChatBox/PlayerChart/Admin review/appeals/TimelineSection);删 useTheme.ts 空壳;删 TimelineSection 重复 .card-hover
5. 容器类统一;npm run build 验证;commit + push origin(dev,默认仅 Gitea)

纯前端改动,不动后端。完成后你验收,再启动第二期(液态玻璃视觉升级)。